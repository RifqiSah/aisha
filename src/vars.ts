import { Collection } from 'discord.js';

export const commandCategories = new Collection();
export const commands = new Collection();
export const interactionCommands = new Collection();
export const autocompletes = new Collection();
export const subCommands = new Collection();