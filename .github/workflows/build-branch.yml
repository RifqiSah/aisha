name: build-branch

on:
  push:
    branches: [ '!master' ]

jobs:
  test:
    uses: ./.github/workflows/test.yml

  dockerize:
    needs: [test]
    uses: ./.github/workflows/build.yml
    with:
      username: ${{ secrets.GH_USERNAME }}
      password: ${{ secrets.GH_PASSWORD }}
      repo: ghcr.io/rifqisah/aisha